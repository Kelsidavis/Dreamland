# Dreamland — It doesn't exist.

Local AI assistant powered by MLX on Apple Silicon.

## Stack
- Python 3.11+, MLX/mlx-lm for inference
- WebSocket (websockets) + HTTP (Starlette/Uvicorn) gateway
- Rich for CLI, Pydantic for config, Click for commands
- JSON file persistence for conversations and memories

## Structure
- src/dreamland/agent/ — MLX runtime, streaming, tool parsing, context window, project context
- src/dreamland/gateway/ — WebSocket + HTTP server, session management
- src/dreamland/skills/ — Skill registry, 4 builtins (filesystem, shell, web, memory), auto-loader
- src/dreamland/memory/ — Persistent agent memory across sessions
- src/dreamland/persistence/ — Conversation store, search, export
- src/dreamland/cli/ — CLI commands + chat slash commands
- src/dreamland/web/ — Single-page chat UI with sidebar and session persistence

## Conventions
- All async, event-driven architecture
- Config in TOML (~/.dreamland/config.toml)
- Agent profiles for model/persona switching
- Hitchhiker's Guide to the Galaxy theming throughout
